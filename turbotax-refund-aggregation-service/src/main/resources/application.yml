spring:
  application:
    name: turbotax-refund-status-aggregate-service

server:
  port: 7002
  error:
    include-message: always

logging:
  level:
    root: INFO
    com.intuit.turbotax: DEBUG
  file:
    name: /home/mgarg/projects/intuit/logs/turbotax-refund-aggregation-service.log
